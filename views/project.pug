extends layout.pug

block content

  nav.nav
    a(href='/')
      span &larr;
      |  Back

  article.portfolio-projects
    .inner-wrapper.flex-row-wrap.two-col
      .project-info-box.box

        //- Use the info in your data file and the comments below to complete this template

        //- Use interpolation to dynamically add the project title
        h1 #{data.projects[id - 1].project_name}

        //- Use interpolation to dynamically add the project description
        p #{data.projects[id - 1].description}

      .project-tech-links-box.box
        h6 Technologies
        ul
          //- Use an `each` iterator and interpolation to dynamically replace the li tags below with the technologies listed for this project
          each technology in data.projects[id - 1].technologies
            li #{technology}

        //- Add the href values to the links below
        a.btn-link(href=`https://gavinbroekema.github.io/techdegree-project-${id}/`, target="_blank") Live Demo
        a.btn-link(href=`https://github.com/gavinbroekema/techdegree-project-${id}`, target="_blank") GitHub Repo

    .project-img-box.box

      //- Replace the URLs in the src attributes below with interpolated template literals to dynamically add your project images, and add descriptions to the alt attributes
      img.project-img(src=`${data.projects[(id - 1)].image_urls[1]}`, alt='')
      img.project-img(src=`${data.projects[(id - 1)].image_urls[2]}`, alt='')
      img.project-img(src=`${data.projects[(id - 1)].image_urls[3]}`, alt='')

    nav.nav
      a(href='/')
        span &larr;
        |  Back
